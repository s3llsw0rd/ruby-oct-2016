<!DOCTYPE html>
<html>
    <head>
        <meta charset="utf-8">
        <title>users</title>
        <link href="https://maxcdn.bootstrapcdn.com/bootstrap/3.3.6/css/bootstrap.min.css" rel="stylesheet" crossorigin="anonymous">
    </head>
    <body>
        <div class="container">
            <h1>Listing users</h1>
            <table class="table table-striped ">
                <thead>
                  <tr>
                    <th>First name</th>
                    <th>Last name</th>
                    <th>Email</th>
                    <th></th>
                  </tr>
                </thead>
                <tbody>
                    <% @users.each do |user| %>
                      <tr>
                        <td><%=user.first_name%></td>
                        <td><%=user.last_name%></td>
                        <td><%=user.email_address%></td>
                        <td>

                            <a class="nav-link bdr-r" href="<%=show_user_path(user.id)%>">Show</a>
                            <a class="nav-link bdr-r" href="<%=edit_user_path(user.id)%>">Edit</a>
                            <form action="<%=delete_user_path(user.id)%>" method="post" class="link">
                                <input type="hidden" name="id" value="<%=user.id%>">
                                <input type="submit" name="submit" value="Destroy" class="nav-link">
                                <input type="hidden" name="_method" value="delete">
                                <input name="authenticity_token" value="<%= form_authenticity_token %>" type="hidden">
                            </form>

                        </td>
                      </tr>
                    <% end %>
                </tbody>
            </table>
            <a href="<%=new_user_path%>">New User</a>
        </div>
    </body>
</html>
